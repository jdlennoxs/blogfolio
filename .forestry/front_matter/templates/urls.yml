---
label: Urls
hide_body: true
fields:
- name: url
  type: field_group_list
  fields:
  - name: name
    type: text
    config:
      required: false
    label: Name
  - name: url
    type: text
    config:
      required: false
    label: URL
  config:
    min: 
    max: 
    labelField: 
  label: URL
